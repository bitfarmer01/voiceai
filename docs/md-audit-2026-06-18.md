# Markdown documentation audit — 2026-06-18

A one-pass inquiry into every repo Markdown doc, surfacing contradictions, stale data, and
misaligned claims between the docs and the actual code/git state. **Every consequential claim here
was verified directly against `git` and the source files** — three earlier audit passes disagreed
with each other and produced several false positives (see section C), so nothing below is taken on
an agent's say-so.

Branch at time of audit: `feature/byod-try-page`. Status legend: **FIXED** (corrected this pass) ·
**LEFT** (intentional, no change) · **FALSE POSITIVE** (an earlier audit claim that did not hold up).

## Inventory (ground truth)

Ten repo `.md` files (excluding `.claude/` skills and worktrees). T = tracked, U = untracked:

| File | Tracked | Role |
|---|---|---|
| `AGENTS.md` | T | Agent/UI-component rules (authoritative) |
| `CLAUDE.md` | T | 1-line `@AGENTS.md` re-export (Claude-Code entrypoint) |
| `designtaste.md` | U | Generic anti-slop frontend skill (reusable, not project state) |
| `memory.md` | T | Session state / locked decisions |
| `plan.md` | T | VAPI receptionist product spec + phase roadmap (source of truth) |
| `ui-development-plan.md` | T | Receptionist UI blueprint (Stitch/Lovable pipeline) |
| `docs/superpowers/plans/2026-06-17-phase1-core-call-loop.md` | U | Phase 1 plan |
| `docs/superpowers/plans/2026-06-17-phase3-make-the-trace-real.md` | T | Phase 3 telemetry plan |
| `docs/superpowers/plans/2026-06-17-ws0-5-conformance-harness-and-vapi-normalizer.md` | T | WS0.5 plan |

`design-guide.md` was **deleted** this pass (see A.1). The session-start `git status` snapshot was
itself stale — it listed `docs/ui-rebrand-plan.md`, `…-phase2-grounding-and-rebrand.md`, and
`…-ws1-ui-feature-build.md` as untracked, but **none exist on disk** (ui-rebrand was deleted earlier;
phase2/ws1 were merged to `main` via PR #2/#4).

## A. Contradictions / wrong content

1. **`design-guide.md` was the wrong product. → FIXED (deleted).** It was titled *"Braindump – UI
   Development Plan (Phase I MVP)"* — a journaling/brain-dump app (signup, onboarding, brain-dump
   screens), unrelated to this VAPI voice receptionist, yet tracked in git. Removed via `git rm`
   (recoverable from history).

2. **`ui-development-plan.md` §2 design system contradicted locked Signal Bold. → FIXED.** It asserted
   indigo `#4F46E5`/violet + Geist Sans; locked identity is amber `#EA580C` + Space Grotesk / Hanken
   Grotesk / IBM Plex Mono. Ground truth: `app/globals.css:93` `--primary: #ea580c`; `app/layout.tsx`
   loads `Hanken_Grotesk`/`Space_Grotesk`/`IBM_Plex_Mono`. §2 was rewritten to mirror those actual
   token/font values, and the §4 Stitch generation prompts (#0 foundation, #1 landing, #2 /try) were
   updated so pasting them no longer regenerates the old palette/fonts. `lucide icons` → `Phosphor
   icons` in prompts #5/#6/#7.

3. **`ui-development-plan.md` referenced deleted components as build targets. → FIXED.**
   `LiveSignalChip`, `RecentCallTicker`, the `HeroProductPeek`/orb-on-gradient hero visual, and the
   `AgentStage` orb base — all retired. Ground truth: `components/landing/` is empty; the three files
   show `D` in git. References were purged or repointed to `VoiceVisualizer`; §3.1 (landing) was
   reconciled to the rebuilt Signal Bold layout (asymmetric hero with a `VoiceVisualizer` demo, no
   live-count widgets), including its Layout/Interactions/States/Data lines.

4. **`memory.md` contradicted itself on BYOD's commit state. → FIXED.** The top warning listed
   "BYOD + appointment UI" as an *uncommitted* work-stream ("None committed"), while the code-review
   section reviewed BYOD over the *committed* range `f7d747e..35e1d30`. Ground truth: BYOD **is
   committed** (`b77d97f`→`35e1d30`). The warning now splits *committed-but-not-on-main* (Phase 3
   telemetry `1e62781` + BYOD) from *uncommitted working tree* (Signal Bold UI, booking, call-end fix,
   providerStats / use-vapi-call hardening, and the dummy-data cleanup).

5. **Phase-3 label collision. → FIXED (in `memory.md`).** `plan.md` defines **Phase 3 =
   observability/telemetry** (now committed as `1e62781`); `memory.md` reused "Phase 3" for the
   **/try + dashboards UI** pass. The memory next-step was relabeled to "UI completion pass — /try +
   dashboards" with a note that it is a separate track from plan.md's telemetry Phase 3.

6. **`memory.md` open question referenced non-existent docs. → FIXED.** It asked about "leftover blue
   sections inside the phase2/ws1 superpowers plans." Those plan docs do not exist, and a grep across
   the surviving plans (phase1/phase3/ws0.5) found **no blue/indigo references**. The question was
   rewritten to drop the false premise and instead flag `docs/stitch-reference/` (9 PNG mockups likely
   rendered in the pre-Signal-Bold indigo palette) as the real open decision.

## B. Stale data

7. **`phase3-make-the-trace-real.md:5` status line said "Not yet committed." → FIXED.** It was
   committed as `1e62781`. Ground truth: `git branch --contains 1e62781` → `feature/byod-try-page` +
   `phase3-make-the-trace-real` (not `main`). Status now reads "Committed as `1e62781` … NOT yet merged
   to `main`."

## C. Verified NON-issues (earlier-audit false positives — recorded so they are not "fixed" later)

- **FALSE POSITIVE: "Phase 3 telemetry is already on `main`, so the memory warning is stale."**
  `git merge-base --is-ancestor 1e62781 main` exits **1** — `1e62781` is NOT an ancestor of `main`.
  The `memory.md` "Convex dev is AHEAD of main / merges MUST include `1e62781`" warning is **correct**
  and was deliberately preserved.
- **FALSE POSITIVE: "`docs/ui-rebrand-plan.md` still exists."** `ls` → no such file. Already deleted;
  `memory.md`'s past-tense note about it is accurate.
- **FALSE POSITIVE: "Blue `#2563eb` / deleted components are still referenced in the codebase."** Those
  appear only under `.claude/worktrees/` (abandoned, isolated agent branches), not in repo docs or
  `app/`. `app/globals.css` uses amber `#ea580c` (light) / `#f97316` (dark) and no blue.
- **FALSE POSITIVE: "designtaste.md contradicts Signal Bold."** It is a generic anti-slop skill and
  *aligns* (prefers Phosphor over lucide, bans Fraunces as a default, mandates one locked accent). Its
  "blue glow discouraged" / "cobalt+cream" / "electric blue" lines are generic color-theory examples,
  not a design direction for this project.

## D. Redundancy (intentional — LEFT)

- `CLAUDE.md` is a 1-line `@AGENTS.md` re-export. This is the standard Claude-Code entrypoint
  convention, not bloat. No change.
- `designtaste.md` (≈1200 lines), `AGENTS.md`, and `plan.md` are deliberate, tight, and authoritative
  reference/spec docs — out of scope for "streamlining" (which targeted stale/wrong content, not length).

## Net changes this pass

- Deleted: `design-guide.md`.
- Edited: `ui-development-plan.md` (§2 + Stitch prompts + dead-component purge), `memory.md`
  (committed/uncommitted split, Phase-3 relabel, stale open-question), `phase3-make-the-trace-real.md`
  (status line).
- Added: this audit artifact.
- No code files touched; docs-only change, `pnpm typecheck` unaffected.
